window.APP_CONFIG = {
  // Example: "https://rebemzjhmzmlkvlgdjgs.supabase.co"
  supabaseUrl: "",
  // Use either anon key or new publishable key (never service_role in frontend)
  supabaseAnonKey: "",
  // Optional: use this instead of supabaseAnonKey if your project uses publishable keys
  supabasePublishableKey: "",
};
